services:
  santra:
    container_name: "santra"
    build: .
    image: ashishwayachal12/santra:${BUILD_NUMBER}
    restart: always
    ports:
      - "1111:80"
    #volumes:
     # - "zipper-vol:/usr/share/nginx/html"
    networks:
      - "santra-net"

#volumes:
 # zipper-vol:
  #  name: "zipper-vol"

networks:
  santra-net:
    name: "santra-net"

